import { Component } from "@angular/core";

// vue (qui prend une partie de l'écran) qui va avec les données qui viennent de la class
@Component({
  selector: "app-root",
  templateUrl: "app.component.html",
})

// donnée de la vue
export class AppComponent{
}
